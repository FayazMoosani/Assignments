// Assign a numeric value representing distance in miles
let miles: number = 50; // You can assign any numeric value here

// Calculate the equivalent distance in kilometers using the conversion factor
const kilometers: number = miles * 1.60934;

// Print the result using console.log
console.log(`The Distance of ${miles} miles is equivalent to ${kilometers.toFixed(2)} kilometers.`);
